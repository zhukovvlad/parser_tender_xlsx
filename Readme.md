# Парсер и анализатор тендерной документации с использованием LLM

## Описание

Этот проект представляет собой комплексный пайплайн на Python для извлечения данных из структурированных Excel-файлов (XLSX) тендерной документации и их последующего интеллектуального анализа с использованием больших языковых моделей (LLM).

Основная цель — автоматизировать не только получение структурированных данных из таблиц, но и их смысловую обработку: автоматическую **категоризацию** каждого лота и **извлечение ключевых технических параметров** в зависимости от его категории.

## Ключевые особенности

- 🔄 **Database-first архитектура** - регистрация в базе данных перед генерацией файлов
- 🛡️ **Резервный режим** - продолжение работы при недоступности сервера
- 📊 **Уникальные ID** - все файлы именуются по ID из базы данных
- 🔧 **Гибкая конфигурация** - поддержка переменных окружения и локальных настроек
- 📝 **Comprehensive logging** - подробное логирование всех операций

## Общий процесс обработки данных

Пайплайн состоит из двух основных этапов:

### 1. Этап 1: Первичная обработка и регистрация (`parse.py`)

**Database-first подход:**
1. **Парсинг XLSX** - извлечение данных из исходного файла
2. **Постобработка** - нормализация структуры, очистка данных
3. **Регистрация в БД** - отправка данных на Go-сервер для получения уникальных ID
4. **Генерация артефактов** - создание всех файлов с ID из базы данных:
   - Основной JSON-файл (`{db_id}.json`)
   - Общий Markdown-отчет (`{db_id}.md`)
   - Детализированные отчеты по позициям (`{db_id}_{lot_db_id}_positions.md`)
   - Чанки для RAG-систем (`{db_id}_chunks.json`)
5. **Архивация** - перемещение всех файлов в соответствующие директории

**Резервный режим:**
- При недоступности сервера автоматически активируется fallback режим
- Генерируются временные ID для продолжения обработки
- Файлы сохраняются в `pending_sync/` для последующей синхронизации

### 2. Этап 2: Интеллектуальный анализ лотов (`llm_test.py`)

- **Категоризация лота:** Определение основной категории (например, "нулевой цикл", "инженерные сети")
- **Извлечение параметров:** Структурированное извлечение технических параметров
- **Результат:** JSON-файл с извлеченными параметрами

## Структура проекта

```
parser/
├── app/                                    # Основной пакет приложения
│   ├── parse.py                           # Главный скрипт парсинга с database-first логикой
│   ├── constants.py                       # Константы проекта
│   ├── helpers/                           # Вспомогательные модули для парсинга
│   │   ├── postprocess.py                # Постобработка JSON
│   │   ├── read_headers.py               # Чтение заголовков
│   │   ├── read_lots_and_boundaries.py   # Чтение лотов
│   │   └── read_executer_block.py        # Чтение блока исполнителя
│   ├── markdown_utils/                    # Утилиты для Markdown
│   │   ├── json_to_markdown.py           # Конвертация JSON в MD
│   │   └── positions_report.py           # Отчеты по позициям
│   ├── markdown_to_chunks/                # Создание чанков для RAG
│   │   └── tender_chunker.py             # Чанкинг текста
│   └── json_to_server/                    # Взаимодействие с сервером
│       └── send_json_to_go_server.py     # Клиент для Go API
├── llm_test.py                           # Анализ лотов с помощью LLM
├── prompts.py                            # Промпты для LLM
├── Makefile                              # Команды для управления проектом
├── requirements.txt                      # Зависимости Python
├── .env.example                          # Пример конфигурации
├── FALLBACK_MODE.md                      # Документация по резервному режиму
└── README.md                             # Документация проекта
```

### Выходные директории
```
tenders_xlsx/           # Исходные XLSX файлы (переименованы с DB ID)
tenders_json/           # JSON файлы с данными
tenders_md/             # Markdown отчеты
tenders_chunks/         # Чанки для RAG-систем
tenders_positions/      # Детализированные отчеты по позициям
pending_sync/           # Файлы в режиме offline (при недоступности сервера)
├── xlsx/              # Временные XLSX файлы
├── json/              # Временные JSON файлы
├── md/                # Временные MD файлы
├── chunks/            # Временные чанки
└── positions/         # Временные отчеты по позициям
```

## Требования

* Python 3.9+ (рекомендуется 3.12+)
* Go-сервер для регистрации тендеров (опционально с резервным режимом)
* Все необходимые библиотеки перечислены в `requirements.txt`

### Ключевые зависимости
- `openpyxl` - работа с Excel файлами
- `requests` - HTTP запросы к серверу
- `python-dotenv` - управление переменными окружения
- `pathlib` - работа с путями файлов

## Установка

1. **Клонируйте репозиторий:**
   ```bash
   git clone https://github.com/zhukovvlad/parser_tender_xlsx.git
   cd parser_tender_xlsx
   ```

2. **Создайте и активируйте виртуальное окружение:**
   ```bash
   python -m venv venv
   # Linux/macOS:
   source venv/bin/activate
   # Windows:
   venv\Scripts\activate
   ```

3. **Установите зависимости:**
   ```bash
   make install
   # или
   pip install -r requirements.txt
   ```

4. **Настройте переменные окружения:**
   ```bash
   cp .env.example .env
   # Отредактируйте .env файл под ваши настройки
   ```

### Пример конфигурации (.env)
```env
# Основные настройки
GO_SERVER_API_ENDPOINT=http://localhost:8080/api/tenders
GO_SERVER_API_KEY=your_api_key_here

# Резервный режим (для работы без сервера)
PARSER_FALLBACK_MODE=false

# Настройки LLM (для llm_test.py)
OLLAMA_URL=http://localhost:11434/api/chat
OLLAMA_MODEL=mistral
OLLAMA_TOKEN=
```

## Использование

### Быстрый старт с Makefile

```bash
# Показать все доступные команды
make help

# Парсинг файла (обычный режим)
make parse FILE=path/to/tender.xlsx

# Парсинг в offline режиме (без сервера)
make parse-offline FILE=path/to/tender.xlsx

# Запуск веб-сервера
make run

# Установка зависимостей
make install

# Запуск тестов
make test

# Очистка временных файлов
make clean
```

### Детальное использование

#### 1. Парсинг XLSX файла

**Обычный режим (с сервером):**
```bash
# Через Makefile
make parse FILE=./data/tender_document.xlsx

# Прямой запуск
python -m app.parse ./data/tender_document.xlsx
```

**Offline режим (без сервера):**
```bash
# Через Makefile
make parse-offline FILE=./data/tender_document.xlsx

# Через переменную окружения
PARSER_FALLBACK_MODE=true python -m app.parse ./data/tender_document.xlsx
```

#### 2. Анализ лотов с помощью LLM

```bash
python llm_test.py
```

#### 3. Персонализация настроек

Создайте файл `Makefile.local` для локальных настроек:
```makefile
# Makefile.local
HOST = 127.0.0.1
PORT = 8080
```

### Режимы работы

| Режим | Описание | Использование |
|-------|----------|---------------|
| **Обычный** | Регистрация в БД + генерация файлов | `make parse FILE=file.xlsx` |
| **Offline** | Работа без сервера с временными ID | `make parse-offline FILE=file.xlsx` |
| **Fallback** | Автоматическое переключение при сбоях | `PARSER_FALLBACK_MODE=true` |

## Формат выходных данных

### Результат парсинга (`parse.py`)

**Обычный режим (с database ID):**
- `{db_id}.json` - JSON с полной информацией по тендеру
- `{db_id}.md` - Общий Markdown-отчет
- `{db_id}_{lot_db_id}_positions.md` - Детализированные отчеты по лотам
- `{db_id}_chunks.json` - Чанки для RAG-систем
- `{db_id}.xlsx` - Исходный файл (переименован с DB ID)

**Offline режим (с временными ID):**
- `temp_{timestamp}_{hash}.json` - Временные файлы в `pending_sync/`
- `temp_{timestamp}_{hash}_lot_{n}_positions.md` - Временные отчеты по позициям

### Результат анализа LLM (`llm_test.py`)

JSON-файл с извлеченными параметрами:
```json
[
  {
    "category": "нулевой цикл",
    "parameter": "толщина стены в грунте",
    "value": 600,
    "unit": "мм",
    "position": "1.1.1 Устройство стены в грунте"
  },
  {
    "category": "нулевой цикл", 
    "parameter": "объем бетона",
    "value": 125.5,
    "unit": "м³",
    "position": "1.1.2 Бетонирование фундамента"
  }
]
```

### Структура файлов

```
# Обычный режим
tenders_xlsx/12345.xlsx                    # Исходный файл
tenders_json/12345.json                    # Основные данные
tenders_md/12345.md                        # Общий отчет
tenders_chunks/12345_chunks.json           # Чанки для поиска
tenders_positions/12345_67890_positions.md # Отчет по лоту

# Offline режим
pending_sync/
├── temp_1642680000_1234.json            # Данные для синхронизации
├── xlsx/temp_1642680000_1234.xlsx       # Исходный файл
├── json/temp_1642680000_1234.json       # Обработанные данные
└── positions/temp_1642680000_1234_lot_1_positions.md
```

## Архитектурные особенности

### Database-First подход
- **Регистрация прежде всего**: Тендер сначала регистрируется в базе данных
- **Уникальные ID**: Все файлы именуются по ID из БД, исключая коллизии
- **Fail-fast**: При неудачной регистрации обработка прерывается
- **Трассируемость**: Прямая связь между файлами и записями в БД

### Резервный режим (Fallback Mode)
- **Автоматическое переключение**: При недоступности сервера
- **Временные ID**: Генерация уникальных временных идентификаторов
- **Offline хранение**: Файлы в `pending_sync/` для последующей синхронизации
- **Сохранность данных**: Обработка продолжается даже при сбоях сети

### Подробнее о резервном режиме
См. документацию: [FALLBACK_MODE.md](FALLBACK_MODE.md)

```bash
# Включить резервный режим
export PARSER_FALLBACK_MODE=true

# Или через .env файл
echo "PARSER_FALLBACK_MODE=true" >> .env

# Или через Makefile
make parse-offline FILE=tender.xlsx
```

## Интеграция с внешними системами

### Go-сервер API
```bash
# Настройка в .env
GO_SERVER_API_ENDPOINT=http://localhost:8080/api/tenders
GO_SERVER_API_KEY=your_api_key_here
```

**Ожидаемый формат ответа:**
```json
{
  "success": true,
  "db_id": "12345",
  "lots_id": {
    "LOT_1": 67890,
    "LOT_2": 67891
  }
}
```

### RAG-системы
Генерируемые чанки (`{id}_chunks.json`) готовы для использования в RAG-системах:
```json
[
  {
    "chunk_id": "chunk_1",
    "text": "Содержимое чанка для поиска",
    "metadata": {
      "tender_id": "12345",
      "chunk_type": "lot_description"
    }
  }
]
```

## Возможные дальнейшие шаги

Данные, извлеченные в результате работы пайплайна, идеально подходят для:

### 1. Аналитические системы
- **Поиск по параметрам:** "все тендеры, где толщина стены в грунте > 500 мм"
- **Дашборды:** Построение аналитических отчетов по техническим параметрам
- **Сравнение тендеров:** Анализ различий в технических решениях

### 2. Машинное обучение
- **Прогнозирование стоимости:** На основе извлеченных параметров
- **Классификация тендеров:** Автоматическое определение типов работ
- **Аномалии:** Поиск нестандартных технических решений

### 3. Интеграция с другими системами
- **ERP системы:** Автоматическая загрузка данных тендеров
- **RAG-системы:** Использование чанков для поиска по содержимому
- **API:** Предоставление данных через REST/GraphQL интерфейсы

## Устранение неполадок

### Проблемы с сервером
```bash
# Проверка доступности API
curl -X GET http://localhost:8080/health

# Тест в offline режиме
make parse-offline FILE=test.xlsx

# Проверка логов
tail -f logs/parser.log
```

### Проблемы с зависимостями
```bash
# Переустановка зависимостей
make clean
make install

# Проверка версии Python
python --version  # Должна быть >= 3.9
```

### Проблемы с файлами
```bash
# Проверка структуры директорий
ls -la tenders_*/
ls -la pending_sync/

# Очистка временных файлов
make clean
```

## Важные замечания и возможные улучшения

### Текущие ограничения
- **Зависимость от сервера:** В обычном режиме требуется доступность Go-сервера
- **Синхронная обработка:** LLM анализ выполняется последовательно
- **Ручная синхронизация:** Файлы из `pending_sync/` требуют ручной обработки

### Возможные улучшения
- **Асинхронность:** Переход на `asyncio` + `aiohttp` для параллельной обработки
- **Batch операции:** Групповая отправка данных на сервер
- **Автоматическая синхронизация:** Утилита для синхронизации offline файлов
- **Monitoring:** Интеграция с системами мониторинга и алертинга
- **Тестирование:** Расширение покрытия модульными и интеграционными тестами

### Рекомендации по производительности
```bash
# Использование SSD для временных файлов
export TEMP_DIR=/fast/ssd/path

# Увеличение таймаута для больших файлов
export PARSER_TIMEOUT=300

# Batch обработка нескольких файлов
for file in *.xlsx; do
  make parse FILE="$file"
done
```

## Лицензия

Этот проект распространяется под лицензией MIT. См. файл [LICENSE](LICENSE) для получения подробной информации.

## Поддержка

- 📧 **Email:** support@example.com
- 🐛 **Issues:** [GitHub Issues](https://github.com/zhukovvlad/parser_tender_xlsx/issues)
- 📖 **Wiki:** [Project Wiki](https://github.com/zhukovvlad/parser_tender_xlsx/wiki)
- 📋 **Changelog:** [CHANGELOG.md](CHANGELOG.md)

---

> **Примечание:** Этот проект находится в активной разработке. Функциональность может изменяться. Рекомендуется регулярно обновлять зависимости и следить за релизами.