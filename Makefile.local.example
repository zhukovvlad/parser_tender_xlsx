# Пример локальных настроек для Makefile
# Скопируйте этот файл в Makefile.local и измените настройки под себя

# Переопределение хоста (по умолчанию 0.0.0.0)
# HOST = 127.0.0.1

# Переопределение порта (по умолчанию 9000)
# PORT = 8080

# Переопределение модуля приложения (по умолчанию main:app)
# APP_MODULE = my_app:application

# Отключение автоперезагрузки в режиме разработки
# RELOAD = 

# Дополнительные параметры для uvicorn
# EXTRA_ARGS = --log-level debug

# Пример с дополнительными аргументами:
# В основном Makefile нужно будет изменить команду run на:
# @uvicorn $(APP_MODULE) --host $(HOST) --port $(PORT) $(RELOAD) $(EXTRA_ARGS)
